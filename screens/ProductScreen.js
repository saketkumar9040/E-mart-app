import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const ProductScreen = ({navigation,routes}) => {
    console.log(routes)
  return (
    <View>
      <Text>ProductPage</Text>
    </View>
  )
}

export default ProductScreen

const styles = StyleSheet.create({})